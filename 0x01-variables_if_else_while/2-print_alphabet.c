#include <stdio.h>
/**
 * main -Main function
 * Description - Lower Case alphabet letters
 * Return: Void
 */
int main(void)
{
	char c;
	c = a;
	while (c = 'a' && c <= 'z')
		putchar(c);
		++c;
	return (0);
}
