#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * main -Main function
 * Description - prints _putchar followed by new line
 * Return: Void
 */
void putchar_string(char *s)
{
	while (i >= 1 && i != NULL)
	putchar (s[i]);
}
int main(void)
{
	char c[] = "_putchar";
void putchar_string(c);
	return (0);
}
